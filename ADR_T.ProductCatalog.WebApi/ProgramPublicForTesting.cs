namespace ADR_T.ProductCatalog.WebApi
{
    /// <summary>
    /// Clase pública para permitir el testing de integración sin exponer la clase Program interna.
    /// Esta clase no contiene lógica y solo sirve como punto de acceso para WebApplicationFactory.
    /// </summary>
    public class ProgramPublicForTesting
    {
        // Esta clase está vacía intencionalmente.
        // Su único propósito es proporcionar un tipo público para WebApplicationFactory.
    }
}
